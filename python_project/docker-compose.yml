services:
  app:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - .:/work
    command: "tail -F anything"
    ports:
      - 5678:5678
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              capabilities: [ utility, compute, video ]